<?php
/**
 * AWA Motos — PDP FAQ JSON-LD (apenas schema, sem markup visível)
 *
 * Emitido no <head> via `before.head.end` ou similar.
 *
 * @var \GrupoAwamotos\SchemaOrg\Block\ProductFaq $block
 */
declare(strict_types=1);

$json = $block->getFaqSchemaJson();
if ($json === '') {
    return;
}
?>
<script type="application/ld+json">
<?= /* @noEscape */ $json ?>
</script>
