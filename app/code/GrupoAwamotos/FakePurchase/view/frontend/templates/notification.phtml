<?php
/** @var \Magento\Framework\View\Element\Template $block */
declare(strict_types=1);
$viewModel = $block->getData('view_model');
if (!$viewModel->isEnabled()) {
    return;
}
?>
<div id="fake-purchase-notification"
     class="fake-purchase-popup <?= $block->escapeHtmlAttr($viewModel->getPosition()) ?>"
     hidden
     aria-live="polite"
     aria-atomic="true">
    <button type="button" class="fake-purchase-close" aria-label="<?= $block->escapeHtmlAttr(__('Fechar notificação')) ?>">&times;</button>
    <div class="fake-purchase-content">
        <div class="fake-purchase-image">
            <a href="#" class="product-link"><img src="" alt="" width="80" height="80" loading="lazy" decoding="async"></a>
        </div>
        <div class="fake-purchase-info">
            <div class="fake-purchase-message"><span class="buyer-name"></span> de <span class="buyer-city"></span></div>
            <div class="fake-purchase-action">comprou</div>
            <a href="#" class="product-link product-name"></a>
            <div class="fake-purchase-time">há poucos minutos</div>
        </div>
    </div>
</div>
<script type="text/x-magento-init">
{
    "#fake-purchase-notification": {
        "GrupoAwamotos_FakePurchase/js/notification": {
            "config": <?= /* @noEscape */ $viewModel->getConfigJson() ?>,
            "products": <?= /* @noEscape */ $viewModel->getProductsJson() ?>,
            "cities": <?= /* @noEscape */ $viewModel->getCitiesJson() ?>,
            "names": <?= /* @noEscape */ $viewModel->getNamesJson() ?>
        }
    }
}
</script>
