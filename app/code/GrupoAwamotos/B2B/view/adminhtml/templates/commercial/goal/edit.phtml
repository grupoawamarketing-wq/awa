<?php
/** @var \GrupoAwamotos\B2B\CommercialPanel\Block\Adminhtml\Goal\EditForm $block */
$options = $block->getAttendantOptions();
?>
<div class="awa-commercial-form awa-section">
    <form method="post" action="<?= $block->escapeUrl($block->getSaveUrl()) ?>">
        <?= $block->getBlockHtml('formkey') ?>
        <fieldset class="admin__fieldset">
            <div class="admin__field">
                <label class="admin__field-label"><span><?= $block->escapeHtml(__('Vendedora')) ?></span></label>
                <div class="admin__field-control">
                    <select name="attendant_id" class="admin__control-select" required>
                        <option value=""><?= $block->escapeHtml(__('Selecione')) ?></option>
                        <?php foreach ($options as $id => $name): ?>
                            <option value="<?= (int) $id ?>"><?= $block->escapeHtml($name) ?></option>
                        <?php endforeach; ?>
                    </select>
                </div>
            </div>
            <div class="admin__field">
                <label class="admin__field-label"><span><?= $block->escapeHtml(__('Mês (YYYY-MM)')) ?></span></label>
                <div class="admin__field-control">
                    <input type="month" name="period_month" class="admin__control-text"
                           value="<?= $block->escapeHtmlAttr($block->getDefaultPeriod()) ?>" required/>
                </div>
            </div>
            <div class="admin__field">
                <label class="admin__field-label"><span><?= $block->escapeHtml(__('Meta de Faturamento (R$)')) ?></span></label>
                <div class="admin__field-control">
                    <input type="number" step="0.01" min="0" name="revenue_goal" class="admin__control-text" value="0"/>
                </div>
            </div>
            <div class="admin__field">
                <label class="admin__field-label"><span><?= $block->escapeHtml(__('Meta de Contatos')) ?></span></label>
                <div class="admin__field-control">
                    <input type="number" min="0" name="contacts_goal" class="admin__control-text" value="0"/>
                </div>
            </div>
            <div class="admin__field">
                <label class="admin__field-label"><span><?= $block->escapeHtml(__('Meta de Clientes Reativados')) ?></span></label>
                <div class="admin__field-control">
                    <input type="number" min="0" name="reactivated_goal" class="admin__control-text" value="0"/>
                </div>
            </div>
        </fieldset>
        <div class="admin__fieldset-wrapper-actions">
            <button type="submit" class="action-primary">
                <span><?= $block->escapeHtml(__('Salvar Meta')) ?></span>
            </button>
            <a class="action-back" href="<?= $block->escapeUrl($block->getBackUrl()) ?>">
                <?= $block->escapeHtml(__('Voltar')) ?>
            </a>
        </div>
    </form>
</div>
