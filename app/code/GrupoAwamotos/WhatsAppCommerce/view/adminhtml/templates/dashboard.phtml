<?php
/** @var \GrupoAwamotos\WhatsAppCommerce\Block\Adminhtml\Dashboard $block */
$health = $block->getHealthData();
$notifications = $block->getNotificationConfig();
?>
<style>
:root {
    --wa-green:      #2ecc71;
    --wa-amber:      #f39c12;
    --wa-blue:       #3498db;
    --wa-red:        #e74c3c;
    --wa-gray:       #95a5a6;
    --wa-muted:      #666666;
    --wa-bg-subtle:  #f8f9fa;
    --wa-bg-cli:     #f0f0f0;
}
    /* WhatsApp Commerce admin — status tokens */
    .wa-status-ok      { color: var(--wa-green); }
    .wa-status-warn    { color: var(--wa-amber); }
    .wa-status-info    { color: var(--wa-blue); }
    .wa-status-error   { color: var(--wa-red); }
    .wa-status-ok-bold { color: var(--wa-green); font-weight: bold; }
    .wa-status-err-bold{ color: var(--wa-red); font-weight: bold; }
    .wa-active         { color: var(--wa-green); }
    .wa-inactive       { color: var(--wa-gray); }
    .wa-stat-card      { background: var(--wa-bg-subtle); padding: 20px; border-radius: 8px; text-align: center; flex: 1; }
    .wa-stat-value-green { font-size: 32px; font-weight: bold; color: var(--wa-green); }
    .wa-stat-value-blue  { font-size: 32px; font-weight: bold; color: var(--wa-blue); }
    .wa-stat-label     { color: var(--wa-muted); }
    .wa-cli-box        { margin-top: 20px; padding: 15px; background: var(--wa-bg-cli); border-radius: 6px; }
</style>
<div class="whatsapp-dashboard" style="max-width: 900px;">
    <h2 style="margin-bottom: 20px;">WhatsApp Commerce &mdash; Dashboard</h2>

    <div class="admin__fieldset" style="margin-bottom: 30px;">
        <h3>Health Check</h3>
        <table class="admin__table-primary">
            <thead>
                <tr>
                    <th><?= $block->escapeHtml(__('Componente')) ?></th>
                    <th><?= $block->escapeHtml(__('Status')) ?></th>
                    <th><?= $block->escapeHtml(__('Detalhe')) ?></th>
                </tr>
            </thead>
            <tbody>
                <?php foreach ($health['checks'] as $name => $check): ?>
                <tr>
                    <td><strong><?= $block->escapeHtml($name) ?></strong></td>
                    <td>
                        <?php if ($check['status'] === 'ok'): ?>
                            <span class="wa-status-ok">OK</span>
                        <?php elseif ($check['status'] === 'warning'): ?>
                            <span class="wa-status-warn">Warning</span>
                        <?php elseif ($check['status'] === 'info'): ?>
                            <span class="wa-status-info">Info</span>
                        <?php else: ?>
                            <span class="wa-status-error">Error</span>
                        <?php endif; ?>
                    </td>
                    <td><?= $block->escapeHtml($check['message']) ?></td>
                </tr>
                <?php endforeach; ?>
            </tbody>
        </table>
        <p style="margin-top: 10px;">
            <strong><?= $block->escapeHtml(__('Status geral:')) ?></strong>
            <?php if ($health['healthy']): ?>
                <span class="wa-status-ok-bold"><?= $block->escapeHtml(__('Operacional')) ?></span>
            <?php else: ?>
                <span class="wa-status-err-bold"><?= $block->escapeHtml(__('Problemas detectados')) ?></span>
            <?php endif; ?>
            &mdash;
            <small><?= $block->escapeHtml($health['timestamp']) ?></small>
        </p>
    </div>

    <div class="admin__fieldset" style="margin-bottom: 30px;">
        <h3><?= $block->escapeHtml(__('Notificacoes Ativas')) ?></h3>
        <table class="admin__table-primary">
            <thead>
                <tr>
                    <th><?= $block->escapeHtml(__('Evento')) ?></th>
                    <th><?= $block->escapeHtml(__('Status')) ?></th>
                </tr>
            </thead>
            <tbody>
                <?php
                $labels = [
                    'order_placed' => 'Pedido Confirmado',
                    'order_paid' => 'Pagamento Aprovado',
                    'order_shipped' => 'Pedido Enviado',
                    'order_refunded' => 'Reembolso Processado',
                ];
                foreach ($notifications as $key => $enabled): ?>
                <tr>
                    <td><?= $block->escapeHtml($labels[$key] ?? $key) ?></td>
                    <td>
                        <?php if ($enabled): ?>
                            <span class="wa-active"><?= $block->escapeHtml(__('Ativo')) ?></span>
                        <?php else: ?>
                            <span class="wa-inactive"><?= $block->escapeHtml(__('Desativado')) ?></span>
                        <?php endif; ?>
                    </td>
                </tr>
                <?php endforeach; ?>
            </tbody>
        </table>
    </div>

    <?php if (isset($health['checks']['optin_stats']['data'])): ?>
    <?php $stats = $health['checks']['optin_stats']['data']; ?>
    <div class="admin__fieldset">
        <h3><?= $block->escapeHtml(__('Opt-in LGPD')) ?></h3>
        <div style="display: flex; gap: 20px;">
            <div class="wa-stat-card">
                <div class="wa-stat-value-green"><?= (int) $stats['opted_in'] ?></div>
                <div class="wa-stat-label"><?= $block->escapeHtml(__('Clientes Opt-in')) ?></div>
            </div>
            <div class="wa-stat-card">
                <div class="wa-stat-value-blue"><?= (int) $stats['total_records'] ?></div>
                <div class="wa-stat-label"><?= $block->escapeHtml(__('Registros de Consentimento')) ?></div>
            </div>
        </div>
    </div>
    <?php endif; ?>

    <div class="wa-cli-box">
        <strong>CLI:</strong>
        <code>bin/magento awa:whatsapp:health</code> &mdash; Health check via terminal<br/>
        <strong>CLI:</strong>
        <code>bin/magento awa:whatsapp:test-send 5516991234567</code> &mdash; Mensagem de teste<br/>
        <strong>API:</strong>
        <code>GET /V1/awa-whatsapp/health</code> &mdash; Health check via REST
    </div>
</div>
