<?php declare(strict_types=1); /** * Home: JSON do storage-manager (sem mage-init no critical path). * * @var \Magento\Catalog\Block\FrontendStorageManager $block */ $storagesConfiguration = json_decode((string) $block->getConfigurationJson(), true); if (!is_array($storagesConfiguration)) { $storagesConfiguration = []; } $payload = json_encode( [ 'component' => 'Magento_Catalog/js/storage-manager', 'appendTo' => (string) $block->getParentComponentName(), 'storagesConfiguration' => $storagesConfiguration, ], JSON_UNESCAPED_UNICODE | JSON_UNESCAPED_SLASHES ); $payload = preg_replace('#</(\s*)script#i', '<\\/\\1script', (string) $payload); ?> <script type="application/json" id="awa-frontend-storage-init-json"><?= /* @noEscape */ $payload ?></script>