<?php
declare(strict_types=1);

/**
 * AWA — Hero LCP Preload (DESATIVADO)
 *
 * O preload dinâmico agora é gerado diretamente pelo template do slider:
 * Rokanthemes_SlideBanner/templates/slider.phtml
 *
 * Isso garante que o preload sempre corresponda à imagem real do primeiro slide,
 * mesmo quando os slides são alterados no admin (Rokanthemes → Slide Banner).
 *
 * O CSS crítico para override do OWL Carousel display:none também está no slider.phtml.
 *
 * @see app/design/frontend/AWA_Custom/ayo_home5_child/Rokanthemes_SlideBanner/templates/slider.phtml
 */

// Nenhum output — preload movido para slider.phtml
