<?xml version="1.0"?>
<page xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:noNamespaceSchemaLocation="urn:magento:framework:View/Layout/etc/page_configuration.xsd">
    <body>
        <!-- Remove original module block (rendered without alias, header.phtml can't reach it) -->
        <referenceBlock name="b2b.header.status.panel" remove="true"/>

        <!--
            NÃO declarar awa.b2b.mode.badge aqui (handle default).
            O Rokanthemes_Themeoption/layout/default.xml deste tema re-declara o bloco
            toki_header DEPOIS deste arquivo (ordem de merge por módulo), e o
            _overrideElementWorkaround do layout descarta todos os filhos já agendados
            sob toki_header/ — o painel nunca chegava à estrutura final.
            A declaração vive em GrupoAwamotos_B2B/layout/customer_account.xml
            (handle processado após o default), cobrindo todas as superfícies de conta.
        -->
    </body>
</page>
