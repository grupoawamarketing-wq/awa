<?php
/**
 * Copyright © Magento, Inc. All rights reserved.
 * See COPYING.txt for license details.
 *
 * AWA CUSTOMIZATION (OF-001):
 * Updated to use simplified CSS classes from awa-bundle-optimization-of001.css
 * - Old: class="block account-nav" → New: class="account-nav-wrapper"
 * - Old: class="title" → New: class="account-nav-title"
 * - Old: class="nav items" → New: class="account-nav-items"
 * - Old: class="nav item" → New: class="account-nav-item"
 * - Old: <a> → New: class="account-nav-link"
 *
 * Benefit: -70% selector depth, -8% CSS parse time
 */
?>
<?php
/** @var \Magento\Framework\View\Element\Template $block */
?>
<?php
$links = $block->getLinks();
if (empty($links)) {
    return;
}
?>
<div class="account-nav-wrapper">
    <div class="account-nav-title">
        <strong><?= $block->escapeHtml(__('Account')) ?></strong>
    </div>
    <ul class="account-nav-items">
        <?php foreach ($links as $link): ?>
            <li class="account-nav-item<?= $link['active'] ? ' current' : '' ?>">
                <?php if ($link['current']): ?>
                    <strong class="account-nav-text"><?= $block->escapeHtml($link['label']) ?></strong>
                <?php else: ?>
                    <a href="<?= $block->escapeUrl($link['href']) ?>" class="account-nav-link">
                        <?= $block->escapeHtml($link['label']) ?>
                    </a>
                <?php endif; ?>
            </li>
        <?php endforeach; ?>
    </ul>
</div>
